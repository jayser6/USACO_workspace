// started : 02/09/22
// finished: ???
// problem : http://hhsprogramming.com/problem?problem=PRB6

#include <bits/stdc++.h>

#define ALL(arr) arr.begin(), arr.end()
#define hashmap unordered_map
#define ll long long
#define pb push_back
#define mp make_pair
#define f first
#define s second
#define endl "\n"

using namespace std;

void solve() {
    string flags, wakeOne, wakeTwo; cin >> flags >> wakeOne >> wakeTwo;

    bool isForward = false, isBackward = false;

    size_t findOne = flags.find(wakeOne);
    size_t findTwo = flags.find(wakeTwo);

    if (findOne > findTwo && ) {
        
    }
}

int main() {
    int t; cin >> t;

    for (int i = 0;i < t;i++) {
        solve();
    }
}
