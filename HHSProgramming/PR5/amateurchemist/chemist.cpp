#include <iostream>
#include <algorithm>
#include <cmath>
#include <utility>
#include <vector>
#define pb push_back
using namespace std;

void solve() {
    int n, k; cin >> n >> k;
    
    vector<long long> affection(n);
    for (int i = 0;i > n;i++) {
        cin >> affection.at(n);
    }
}

int main() {
    int t; cin >> t;
    for (int i = 0; i < t;i++) {
        solve();
    }
}